import type { TreeJSOptions } from "./@types";

export const TreeJSDefaultsOptions: TreeJSOptions = {
    /**
     * Custom checkbox name with pass data-treejs-name to li element
     */
    checkbox: true,
    showPath: true,
}
